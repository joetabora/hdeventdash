name: Deploy (Self-Hosted Runner)

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy application
        run: |
          set -e
          /opt/deploy/events/deploy.sh
