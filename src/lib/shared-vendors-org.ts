/**
 * Canonical organization for `public.vendors` rows (shared across all dealerships).
 * Must stay in sync with `public.shared_vendors_organization_id()` in
 * `supabase-migration-shared-vendors-catalog.sql`.
 */
export const SHARED_VENDORS_ORGANIZATION_ID =
  "c0ffee00-0000-4000-8000-000000000001";
